[package]
name = "dioxus-code-content-editable"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
dioxus = { version = "0.7.0" }
dioxus-code = { path = "../..", features = ["runtime"] }

[features]
default = ["desktop"]
desktop = ["dioxus/desktop"]
web = ["dioxus/web"]
